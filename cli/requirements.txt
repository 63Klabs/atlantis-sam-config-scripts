# Runtime dependencies for the cli/ scripts (shipped to end users).
#   pip install -r cli/requirements.txt
#
# Test and dev tooling live in cli/requirements-test.txt and
# cli/requirements-dev.txt and are NOT installed by end users.

# AWS SDK and CLI tools
boto3>=1.28.0
botocore>=1.31.0
aws-sam-cli>=1.95.0

# Configuration and formatting
toml>=0.10.2
tomli>=2.2.1
tomlkit>=0.12.0
click>=8.1.0

# Type hints support
typing-extensions>=4.7.1
